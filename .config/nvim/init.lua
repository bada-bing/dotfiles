-- require only the "user" and all other requires do inside user's init.lua file
require "miki" -- change user to more meaningful name (e.g., miki)

-- Structure of the project:
-- lua/user -- where the custom settings are
-- plugin/ -- the plugins which are loaded when the neovim is started
-- https://stackoverflow.com/a/14248628
